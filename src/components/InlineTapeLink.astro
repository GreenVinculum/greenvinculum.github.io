---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a href={href} target="_blank" rel="noopener noreferrer" class="inline-link-highlight">
  <span class="inline-link-label" set:html={label} />
</a>
